import { Logo } from "../assets/Logo";

export const Header = () => {
  return (
    <header className="py-[4.75rem] bg-gray-700 flex items-center justify-center">
      <Logo />
    </header>
  );
};
